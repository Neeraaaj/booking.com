/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'r-xx.bstatic.com',
      'cf.bstatic.com',
      'example.com', // Add additional domains here
    ],
  },
}

module.exports = nextConfig
